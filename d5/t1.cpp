#include <bits/stdc++.h>
using namespace std;

int main() {
    while (cin) {
        int x1, y1, x2, y2;
        cin >> x1 >> y1 >> x2 >> y2;
        cout << x1 << ' ' << y1 << ' ' << x2 << ' ' << y2 << '\n';
        if (cin) {
            cout << x1 << ' ' << y1 << ' ' << x2 << ' ' << y2 << '\n';
        }
    }
    return 0;
}
