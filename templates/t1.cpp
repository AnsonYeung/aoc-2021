#include <bits/stdc++.h>
using namespace std;

int main() {
    vector<string> inputs;
    while (cin) {
        string s;
        cin >> s;
        if (cin) {
            inputs.push_back(s);
        }
    }



    return 0;
}
